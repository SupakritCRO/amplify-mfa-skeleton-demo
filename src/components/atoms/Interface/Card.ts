export interface EstimatedCardProps {
    borderColor: string
    children?: React.ReactNode
    height?: string
    minWidth?: string
    padding: string
  }

export interface RiskScoreItemProps {
  title?: string,
  result: string,
  bgColor: string,
  height?: string,
}